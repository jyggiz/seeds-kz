import A01ImageProps from '../../atom/a01-image/A01Image.types';

export type M51RegionPropertyProps = {
  scrollComponent?: boolean;
  image: A01ImageProps;
  heading: string;
  description: string;
};
